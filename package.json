{
  "name": "udcpinepdx",
  "private": true,
  "workspaces": ["shared", "web/frontend", "web/blog"],
  "scripts": {
    "lint": "bun run --filter '*' lint",
    "test": "bun run --filter '*' test"
  }
}
