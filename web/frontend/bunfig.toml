[test]
# happy-dom registers `window`, `document`, etc. on globalThis so preact's
# render() and @testing-library/preact work under `bun test`. Loaded once
# per test process via preload.
preload = ["./bunfig-test-setup.ts"]
